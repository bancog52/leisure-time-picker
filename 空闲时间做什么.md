# 空闲时间做什么

## 方案名称

**“空闲时间做什么”抽签网页（GitHub Pages + 纯前端）**

## 一、项目目标

- 做一个简单的网页，展示你列出的一系列“空闲时间能做什么”的活动选项。
- 有一个“抽签按钮”，点击后**从列表中随机选一个活动**，动态显示在页面上。
- 整个网页通过 GitHub Pages 托管，不需要后端服务器。

------

## 二、技术要求

- 使用 **纯前端技术**：
  - `HTML`：页面结构，采用现代和语义化标签。
  - `CSS`：**极佳的视觉与氛围感（重点要求）**。采用毛玻璃风格（Glassmorphism）搭配治愈系、缓慢流动的渐变色彩背景。并使用现代无衬线字体排版。
  - `JavaScript`：处理按钮点击、随机抽取、控制滚动抽签动效和结果平滑展示。
- 项目托管在 **GitHub Pages**，仅包含静态文件且支持 PWA（添加到主屏幕），无需后端。

------

## 三、功能需求

1. **显示活动列表**
   - 在页面上用一个简单的区块（例如 `<div></div>` 或 `<ul>` 列表）展示你提供的所有活动（用中文原文）。
   - 示例活动列表（你可以直接复制给 AI 用）：
     - 公园散步
     - 喝水/泡茶/咖啡
     - 拉开窗帘，看看天空
     - 看窗外发呆
     - 关掉手机/电脑
     - 冥想/内观
     - 听音乐/白噪音
     - 躺着
     - 写日记
     - 泡澡/泡脚
     - 收拾房间
     - 洗衣服
     - 浅社交（给朋友发一条轻松消息、拍一张照片、感受一下外面的空气）
     - 纪录片/电影
     - 手工
     - 读书
     - 运动
     - 美学/话剧
     - 摄影
2. **抽签按钮功能**
   - 页面上有一个按钮，文字为：
     - 可选文案：`“抽一个空闲时间做的事”` 或 `“抽签”`。
   - 按钮点击后：
     - 不要生硬地立刻出结果，而是先触发 1~2 秒的“文字快速滚动出老虎机效果”或“卡片翻转”等待动画拉满期待。
     - 动画结束后，从上述活动列表中**最终定格并随机抽取一个**。
     - 完全使用原生 JavaScript 实现。
   - 抽签结果要**动态优雅地显示在页面上**，例如：
     - 带有淡入（Fade-in）过渡特效的中心毛玻璃区块 `<div id="result">` 显示当前抽中的活动。
3. **交互行为**
   - 每次点击按钮，重置并重新播放滚动动效，最后覆盖前一次结果。
   - 拥有微交互细节（如按钮的呼吸悬浮光晕）。
   - 不需要保存历史记录等后端逻辑，纯本地运转。
4. **页面结构示意（让 AI 自己组织 HTML）**
   - 一个标题，例如：
     - `h1`：“空闲时间能做什么？”
   - 一个按钮：`<button id="draw">...</button>`
   - 一个结果展示区：`<div id="result"></div>`
   - 可选：把活动列表以“小建议”的方式排版在页面其他位置，但不强制交互。
5. **样式与美感要求（请注意氛围感）**
   - 代码须包含高质量的全局色彩变量、毛玻璃参数。
   - 重视动画与缓动效果（Transitions & Keyframes）。
   - **完美支持响应式（手机端为主适配）**，确保在手机 Safari / 微信中打开同样精致美观。

------

## 四、部署说明（给 AI 写成“Readme” 或只是备注）

- 项目包含：
  - `index.html`（主页面，包含 HTML、CSS、内联 JS）
- 使用 GitHub Pages 托管：
  - 创建 GitHub 仓库，把 `index.html` 放在主分支（如 `main`）。
  - 在 Settings → Pages 中选择该分支，启用 GitHub Pages。
  - 最终访问地址格式为：`https://用户名.github.io/仓库名`。

------

## 五、额外说明

- 所有逻辑必须在**前端 JavaScript 实现**，不要引入后端服务、数据库或 API。
- 不需要登录、用户系统、数据持久化等复杂功能。
- 最终目标是：**把这份方案交给 AI，让它生成一份完整的 `index.html` 文件，可直接上传到 GitHub Pages 运行。**

------

